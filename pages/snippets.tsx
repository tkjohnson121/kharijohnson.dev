import { Layout } from '../common';

const IndexPage = () => {
  return (
    <Layout seo={{ title: 'Snippets' }}>
      <h1 className="text-6xl font-bold">Snippets.</h1>
    </Layout>
  );
};

export default IndexPage;
